<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #e95fd2;">
    <div class="container-fluid">
        <!-- Brand -->
        <a class="navbar-brand" href="/" style="font-weight: bold; color: #ffd500;">KPCMS</a>

        <!-- Toggler/collapsible Button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar links -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <!-- Main menu items -->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <% if (isAuthenticated) { %>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/" style="color: #ffd500;">Home</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="/cards" style="color: #ffd500;">Cards</a>
                    </li>

                    <% if (isAdmin) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/users" style="color: #ffd500;">Users</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link" href="/applications" style="color: #ffd500;">Applications</a>
                        </li>
                    <% } %>
                <% } %>
            </ul>

            <!-- Authentication links -->
            <ul class="navbar-nav">
                <% if (!isAuthenticated) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login" style="color: #ffd500;">Login</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="/users/new" style="color: #ffd500;">Register</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout" style="color: #ffd500;">Logout</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
